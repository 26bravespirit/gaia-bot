# Gaia-Bot 架构图

## 功能架构图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           Gaia Control Center (:3400)                       │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌─────────────────┐  │
│  │ PM2 管理  │ │ 主动发言  │ │ 随系统启动│ │ 定时关闭  │ │ Pipeline/Memory │  │
│  │ 启停/重启 │ │ 开关     │ │ launchd  │ │ 日定时/延时│ │ Logs/Errors    │  │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘ └─────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                              Lark 通道层                                    │
│                                                                             │
│  ┌─────────────────┐  ┌──────────────────┐  ┌───────────────────────────┐  │
│  │  ChannelManager  │  │   LarkChannel    │  │       LarkClient          │  │
│  │  多通道注册/启停  │──│  WebSocket 订阅   │  │  sendText / replyInThread │  │
│  │                  │  │  NDJSON 解析      │  │  getMessages / sendCard   │  │
│  └─────────────────┘  │  extractLarkMsg() │  │  getUserName              │  │
│                        └──────────────────┘  └───────────────────────────┘  │
└───────────────────────────────┬──────────────────────────────────────────────┘
                                │ LarkMessage
                                ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                           消息预处理层 (index.ts)                            │
│                                                                             │
│  ┌──────────┐  ┌──────────────┐  ┌──────────────┐  ┌───────────────────┐  │
│  │  去重     │  │ 过期消息丢弃  │  │ 引用消息拉取  │  │  发送者名称解析   │  │
│  │ isSeen() │  │ >5min drop   │  │ getMessages()│  │  cache→DB→API     │  │
│  └──────────┘  └──────────────┘  └──────────────┘  └───────────────────┘  │
└───────────────────────────────┬──────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                     MessageCoalescer (消息合并器)                            │
│                                                                             │
│  收集同一用户的连续消息 → 静默2s或最长8s后触发 → @bot立即触发                  │
│  传播: rootId / chatType / mentions                                         │
└───────────────────────────────┬──────────────────────────────────────────────┘
                                │ CoalescedMessage
                                ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                         8 阶段 Pipeline                                     │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ S1: MessageDispatcher                                               │   │
│  │ 过滤自身消息 · @mention检测 · @他人跳过 · 非文本合成 · 文本清洗       │   │
│  └─────────────────────────────────┬───────────────────────────────────┘   │
│                                    ▼                                       │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ S2: ContextAssembler                                                │   │
│  │ 用户画像 · 对话历史 · 时间状态 · 传记上下文 · 长期记忆 · 关系状态     │   │
│  │ 自我状态(情绪/能量/社交电量)                                          │   │
│  └─────────────────────────────────┬───────────────────────────────────┘   │
│                                    ▼                                       │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ S3S4: CognitiveGenerator                                           │   │
│  │                                                                     │   │
│  │  ┌──────────────┐    ┌──────────────────────────────────────────┐  │   │
│  │  │ 认知决策      │    │         LLM + Tool Loop (≤3轮)          │  │   │
│  │  │ · 传记话题   │    │                                          │  │   │
│  │  │ · 身份挑战   │    │  callLLM(messages, tools, rawItems)     │  │   │
│  │  │ · 回应策略   │    │       │                                  │  │   │
│  │  └──────────────┘    │       ▼ function_call?                   │  │   │
│  │  ┌──────────────┐    │  ┌─────────┐    ┌──────────────────┐    │  │   │
│  │  │ 人类行为注入  │    │  │ Yes     │───▶│  Tool Executor   │    │  │   │
│  │  │ · 反驳       │    │  └─────────┘    │  · web_search    │    │  │   │
│  │  │ · 装糊涂    │    │       │          │  · read_url      │    │  │   │
│  │  │ · 苏格拉底  │    │       ▼          └───────┬──────────┘    │  │   │
│  │  │ · 选择性忽略 │    │  function_call_output ◀──┘               │  │   │
│  │  │ · 情绪拒绝  │    │       │                                   │  │   │
│  │  └──────────────┘    │       ▼ 下一轮 callLLM                   │  │   │
│  │                       │  最终 text → generatedResponse          │  │   │
│  │                       └──────────────────────────────────────────┘  │   │
│  └─────────────────────────────────┬───────────────────────────────────┘   │
│                                    ▼                                       │
│  ┌──────────────────────────────────────────────────────────────────┐      │
│  │ S4.5: BiographicalExtractor        S4.6: MemoryExtractor        │      │
│  │ 正则提取传记事实 (同步)             正则提取记忆 (同步)            │      │
│  │ + 异步批量 LLM 提取                + 异步批量 LLM 提取            │      │
│  │         └──────────┬───────────────────────┘                     │      │
│  │                    ▼                                              │      │
│  │          ExtractionScheduler                                     │      │
│  │          批量5条 或 2分钟触发 → LLM 提取 → 写入 DB                │      │
│  └──────────────────────────────────────────────────────────────────┘      │
│                                    ▼                                       │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ S5: PerceptionWrapper (4步子流水线)                                  │   │
│  │ Step1: Anti-AI规则 R00-R06 (提示词泄露·列举·尾问·套话·长度·知识堆砌) │   │
│  │ Step2: 记忆模糊 (日期·顺序·低重要度细节)                              │   │
│  │ Step3: 不完美注入 (口头禅·语气词·错字·未完成句)                        │   │
│  │ Step4: @提及前缀                                                     │   │
│  └─────────────────────────────────┬───────────────────────────────────┘   │
│                                    ▼                                       │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ S5.5: AntiAiValidator (8维指纹检测)                                  │   │
│  │ 句式规律 · 词汇多样性 · 长度规律 · 连接词频率                         │   │
│  │ 共情模板 · 知识倾倒 · 完整度 · 情感真实性                             │   │
│  │ → 加权评分 → PASS / WARN / BLOCK                                    │   │
│  └─────────────────────────────────┬───────────────────────────────────┘   │
│                                    ▼                                       │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ S6: OutboundScheduler                                               │   │
│  │ 段落拆分 · 回复延迟(模拟打字) · thread回复/主聊天 · 记录到记忆        │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                          工具系统 (src/tools/)                               │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                      Tool Executor (路由层)                          │   │
│  │              executeTool(name, args) → JSON string                   │   │
│  └──────────┬─────────────────────────────────┬────────────────────────┘   │
│             ▼                                 ▼                             │
│  ┌──────────────────────┐          ┌──────────────────────────┐           │
│  │   web_search          │          │      read_url             │           │
│  │   (Tavily API)        │          │   (fetch + html-to-text)  │           │
│  ├──────────────────────┤          ├──────────────────────────┤           │
│  │ POST tavily.com/search│          │ fetch(url)               │           │
│  │ query + max_results   │          │ HTML → 纯文本 (≤2000字)  │           │
│  │ → [{title,url,snippet}]│         │ 跳过 nav/footer/script   │           │
│  │ API Key: TAVILY_API_KEY│         │ → {title, content}       │           │
│  │ 超时: 15s             │          │ 超时: 15s                │           │
│  └──────────────────────┘          └──────────────────────────┘           │
│                                                                             │
│  调用方式: S3S4 Tool Loop → callLLM 返回 function_call → executeTool()     │
│           → function_call_output 回传 → callLLM 生成最终回复 (≤3轮)        │
│  工具定义: TOOL_DEFINITIONS[] 传入 OpenAI Responses API tools 参数          │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                             记忆系统                                        │
│                                                                             │
│  ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌──────────┐ ┌───────────┐ │
│  │ Immediate   │ │  Working   │ │ Long-Term  │ │Biography │ │Relationship│ │
│  │ Memory      │ │  Memory    │ │  Memory    │ │ Facts    │ │  Model    │ │
│  ├────────────┤ ├────────────┤ ├────────────┤ ├──────────┤ ├───────────┤ │
│  │ 进程内滑窗  │ │ SQLite     │ │ 情感事件    │ │ 锚定事实  │ │ stranger  │ │
│  │ 最近20条    │ │ 对话日志    │ │ 承诺       │ │ 生成事实  │ │acquaintance││
│  │            │ │ 用户表      │ │ 共同经历    │ │ 时期分类  │ │ familiar  │ │
│  │            │ │ 运行时配置   │ │ 事实细节    │ │ 重要度    │ │ intimate  │ │
│  │            │ │ 事件日志    │ │ 关键词检索   │ │ 可见性    │ │ 亲密度分数 │ │
│  └────────────┘ └────────────┘ └────────────┘ └──────────┘ └───────────┘ │
│                         ▲ 统一接口: MemoryManager                          │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                             引擎组件                                        │
│                                                                             │
│  ┌────────────┐ ┌─────────────────┐ ┌─────────────────┐ ┌──────────────┐ │
│  │ TimeEngine  │ │ProactiveInitiator│ │IdentityGuardian │ │  EventBus    │ │
│  ├────────────┤ ├─────────────────┤ ├─────────────────┤ ├──────────────┤ │
│  │ 活跃时段    │ │ 每10分钟检查     │ │ 身份边界保护     │ │ 发布/订阅    │ │
│  │ 睡眠模式    │ │ 沉默阈值触发     │ │ 禁止揭示列表     │ │ message_recv │ │
│  │ 能量等级    │ │ 时间段触发       │ │ 回避词过滤       │ │ response_sent│ │
│  │ 回复延迟    │ │ 承诺跟进        │ │ 身份挑战回复      │ │ error        │ │
│  │ 情绪基线    │ │ 日限/冷却/防堆叠 │ │                  │ │ persona_reload││
│  └────────────┘ └─────────────────┘ └─────────────────┘ └──────────────┘ │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                             配置系统                                        │
│                                                                             │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────────────┐ │
│  │  persona.yaml     │  │ constraints.yaml │  │ prompt_mappings.yaml     │ │
│  │  (热重载)         │  │                  │  │                          │ │
│  ├──────────────────┤  ├──────────────────┤  ├──────────────────────────┤ │
│  │ 身份/性格/OCEAN   │  │ Anti-AI R00-R06  │  │ 人类行为指令模板          │ │
│  │ 知识领域           │  │ 关系阶段提示      │  │ 时间上下文指令            │ │
│  │ 语言风格/不完美    │  │ 传记块标记        │  │ 身份挑战警告              │ │
│  │ 时间行为           │  │ 长期记忆标记      │  │                          │ │
│  │ 传记锚点           │  │                  │  │                          │ │
│  │ 降级模板           │  │                  │  │                          │ │
│  └──────────────────┘  └──────────────────┘  └──────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 消息处理时序图

```
用户                  Lark                LarkChannel         index.ts          Coalescer         Pipeline
 │                    │                      │                   │                  │                │
 │  发送消息           │                      │                   │                  │                │
 │──────────────────▶│                      │                   │                  │                │
 │                    │  WebSocket push      │                   │                  │                │
 │                    │─────────────────────▶│                   │                  │                │
 │                    │                      │  NDJSON parse     │                  │                │
 │                    │                      │  extractLarkMsg() │                  │                │
 │                    │                      │  filter non-text  │                  │                │
 │                    │                      │  filter non-user  │                  │                │
 │                    │                      │                   │                  │                │
 │                    │                      │  onMessage(msg)   │                  │                │
 │                    │                      │─────────────────▶│                  │                │
 │                    │                      │                   │                  │                │
 │                    │                      │                   │  handleMessage() │                │
 │                    │                      │                   │  ┌─────────────┐ │                │
 │                    │                      │                   │  │ dedup check │ │                │
 │                    │                      │                   │  │ stale check │ │                │
 │                    │                      │                   │  │ 引用拉取     │ │                │
 │                    │                      │                   │  │ 名称解析     │ │                │
 │                    │                      │                   │  │ addMessage() │ │                │
 │                    │                      │                   │  └─────────────┘ │                │
 │                    │                      │                   │                  │                │
 │                    │                      │                   │  coalescer.push()│                │
 │                    │                      │                   │────────────────▶│                │
 │                    │                      │                   │                  │                │
 │                    │                      │                   │                  │ 等待2s静默...   │
 │                    │                      │                   │                  │ 或@bot立即触发  │
 │                    │                      │                   │                  │                │
 │                    │                      │                   │                  │  fireBurst()   │
 │                    │                      │                   │                  │───────────────▶│
 │                    │                      │                   │                  │                │
```

```
Pipeline                S1              S2              S3S4            Tools           LLM
 │                      │               │                │               │               │
 │  run(burst)          │               │                │               │               │
 │─────────────────────▶│               │                │               │               │
 │                      │               │                │               │               │
 │                      │ filter/detect │                │               │               │
 │                      │ @mention判断   │                │               │               │
 │                      │ 文本清洗       │                │               │               │
 │                      │──────────────▶│                │               │               │
 │                      │               │                │               │               │
 │                      │               │ 加载用户画像    │               │               │
 │                      │               │ 对话历史        │               │               │
 │                      │               │ 时间/传记/记忆  │               │               │
 │                      │               │ 关系/自我状态   │               │               │
 │                      │               │───────────────▶│               │               │
 │                      │               │                │               │               │
 │                      │               │                │ buildMessages()│               │
 │                      │               │                │ 认知决策        │               │
 │                      │               │                │ 人类行为注入    │               │
 │                      │               │                │               │               │
 │                      │               │                │  callLLM(msg, tools)          │
 │                      │               │                │──────────────────────────────▶│
 │                      │               │                │               │               │
 │                      │               │                │               │   function_call│
 │                      │               │                │◀──────────────────────────────│
 │                      │               │                │               │               │
 │                      │               │                │ executeTool()  │               │
 │                      │               │                │──────────────▶│               │
 │                      │               │                │               │ Tavily/fetch   │
 │                      │               │                │  tool result  │               │
 │                      │               │                │◀──────────────│               │
 │                      │               │                │               │               │
 │                      │               │                │  callLLM(msg, tools, rawItems)│
 │                      │               │                │──────────────────────────────▶│
 │                      │               │                │               │   final text   │
 │                      │               │                │◀──────────────────────────────│
 │                      │               │                │               │               │
```

```
Pipeline         S4.5/S4.6      ExtractionSched       S5              S5.5            S6            Lark
 │                  │                │                  │                │               │              │
 │  generatedResp   │                │                  │                │               │              │
 │────────────────▶│                │                  │                │               │              │
 │                  │ 正则提取        │                  │                │               │              │
 │                  │ 传记+记忆       │                  │                │               │              │
 │                  │ 异步推入        │                  │                │               │              │
 │                  │───────────────▶│ (批量累积)        │                │               │              │
 │                  │                │                  │                │               │              │
 │                  │────────────────────────────────▶ │                │               │              │
 │                  │                │                  │                │               │              │
 │                  │                │                  │ Anti-AI R00-06 │                │               │
 │                  │                │                  │ 记忆模糊        │                │               │
 │                  │                │                  │ 不完美注入      │                │               │
 │                  │                │                  │ @前缀          │                │               │
 │                  │                │                  │───────────────▶│               │              │
 │                  │                │                  │                │               │              │
 │                  │                │                  │                │ 8维AI指纹检测  │              │
 │                  │                │                  │                │ PASS/WARN/BLOCK│              │
 │                  │                │                  │                │──────────────▶│              │
 │                  │                │                  │                │               │              │
 │                  │                │                  │                │               │ 段落拆分      │
 │                  │                │                  │                │               │ 回复延迟      │
 │                  │                │                  │                │               │ thread/主聊天  │
 │                  │                │                  │                │               │              │
 │                  │                │                  │                │               │  sendText()  │
 │                  │                │                  │                │               │  或           │
 │                  │                │                  │                │               │replyInThread()│
 │                  │                │                  │                │               │─────────────▶│
 │                  │                │                  │                │               │              │
 │                  │                │                  │                │               │ addMessage() │
 │                  │                │                  │                │               │ (记录回复)    │
 │                  │                │                  │                │               │              │
```

## 主动发言时序图

```
setInterval(10min)     ProactiveInitiator     MemoryManager       LarkChannel        Lark
      │                       │                     │                  │                │
      │  check()              │                     │                  │                │
      │──────────────────────▶│                     │                  │                │
      │                       │                     │                  │                │
      │                       │  runtime_config     │                  │                │
      │                       │  proactive_enabled? │                  │                │
      │                       │────────────────────▶│                  │                │
      │                       │◀────────────────────│                  │                │
      │                       │                     │                  │                │
      │                       │  getDailyCount()    │                  │                │
      │                       │  hasUnanswered?     │                  │                │
      │                       │  isRecentlyActive?  │                  │                │
      │                       │────────────────────▶│                  │                │
      │                       │◀────────────────────│                  │                │
      │                       │                     │                  │                │
      │                       │  lastMsg by user_id │                  │                │
      │                       │  计算沉默时长         │                  │                │
      │                       │  评估触发条件         │                  │                │
      │                       │                     │                  │                │
      │  ProactiveMessage     │                     │                  │                │
      │◀──────────────────────│                     │                  │                │
      │                       │                     │                  │                │
      │  channel.sendText()   │                     │                  │                │
      │─────────────────────────────────────────────────────────────▶│                │
      │                       │                     │                  │  sendText()    │
      │                       │                     │                  │───────────────▶│
      │                       │                     │                  │                │
      │  memory.addMessage()  │                     │                  │                │
      │─────────────────────────────────────────────▶                  │                │
      │                       │                     │                  │                │
```


## 大脑工作图

图 1｜类大脑 AI 系统总架构

                         ┌───────────────────────────┐
                         │        用户 / 环境输入      │
                         │ text / voice / image / event │
                         └─────────────┬─────────────┘
                                       │
                                       ▼
                    ┌──────────────────────────────────┐
                    │   输入网关 / Thalamus（丘脑）      │
                    │   接收、分发、限流、鉴权、打标签    │
                    └─────────────┬────────────────────┘
                                  │
              ┌───────────────────┼───────────────────┐
              │                   │                   │
              ▼                   ▼                   ▼
   ┌────────────────┐  ┌────────────────┐  ┌────────────────┐
   │ Vision 模块     │  │ Audio 模块      │  │ Parser/OCR 模块 │
   │ 图像/界面理解    │  │ 语音识别/说话人  │  │ 文档/表格解析    │
   └───────┬────────┘  └───────┬────────┘  └───────┬────────┘
           └───────────────────┴───────────────────┘
                               │
                               ▼
                ┌────────────────────────────────────┐
                │  语义理解层 / Wernicke-like         │
                │  意图识别 / 实体抽取 / 风险判断      │
                │  “这是什么任务？要不要进 agent 模式？” │
                └────────────────┬───────────────────┘
                                 │
                                 ▼
                ┌────────────────────────────────────┐
                │   工作记忆黑板 / Working Memory      │
                │   当前目标 / 已知事实 / 证据 / 状态   │
                │   历史摘要 / 中间结论 / 待办事项      │
                └────────────────┬───────────────────┘
                                 │
                                 ▼
              ┌────────────────────────────────────────────┐
              │    执行控制器 / Prefrontal Cortex          │
              │    Router + Planner + Policy + Orchestrator│
              └───────────────┬────────────────────────────┘
                              │
          ┌───────────────────┴────────────────────┐
          │                                        │
          ▼                                        ▼
┌────────────────────────┐              ┌────────────────────────┐
│ 快速通路 / Workflow     │              │ 慢速通路 / Agent Loop   │
│ 固定流程、低风险任务     │              │ 开放问题、多步探索任务   │
│ 提取/总结/分类/改写      │              │ 计划→执行→观察→修正      │
└───────────┬────────────┘              └───────────┬────────────┘
            │                                       │
            └───────────────────┬───────────────────┘
                                ▼
                 ┌───────────────────────────────────┐
                 │     记忆系统 / Hippocampus         │
                 │ short-term   工作记忆              │
                 │ episodic     任务轨迹/事件日志      │
                 │ semantic     知识库/向量库/图谱      │
                 │ procedural   工具说明/策略模板      │
                 └────────────────┬──────────────────┘
                                  │
                                  ▼
                 ┌───────────────────────────────────┐
                 │   工具网关 / Motor System          │
                 │   API / DB / Browser / Code / RPA │
                 │   权限 / 沙箱 / 重试 / 回滚 / 审批  │
                 └────────────────┬──────────────────┘
                                  │
                                  ▼
                 ┌───────────────────────────────────┐
                 │   输出层 / Broca-like              │
                 │   回复生成 / 行动确认 / UI反馈      │
                 └───────────────────────────────────┘



## 混用 LLM 和 agent

图 2｜请求分流：什么时候走 LLM，什么时候走 Agent

                 ┌───────────────┐
                 │ 用户发来一个任务 │
                 └───────┬───────┘
                         ▼
              ┌─────────────────────┐
              │ Router / Task Judge │
              │ 判断任务复杂度与风险  │
              └─────────┬───────────┘
                        │
        ┌───────────────┼────────────────┐
        │                                   │
        ▼                                   ▼
┌────────────────────┐           ┌────────────────────────┐
│ 可确定、单步、低歧义 │           │ 不确定、多步、需探索决策 │
│ 例如：总结、翻译、抽取 │           │ 例如：调研、排障、执行任务 │
└─────────┬──────────┘           └──────────┬─────────────┘
          ▼                                   ▼
┌────────────────────┐           ┌────────────────────────┐
│ 走 LLM Workflow     │           │ 走 Agent Loop          │
│ prompt + tools      │           │ plan -> act -> observe │
│ 少状态、低成本、快   │           │ retry -> critique      │
└─────────┬──────────┘           └──────────┬─────────────┘
          │                                   │
          └───────────────┬──────────────────┘
                          ▼
               ┌─────────────────────────┐
               │ Verifier / Policy Gate  │
               │ 检查证据、权限、结果一致性 │
               └────────────┬────────────┘
                            ▼
                     ┌──────────────┐
                     │ 最终输出/执行  │
                     └──────────────┘


## “大脑脑区”和“系统组件”的对应关系

图 3｜脑区能力 ↔ 工程组件映射图

┌──────────────────────┬────────────────────────────────────┐
│ 人脑功能模块           │ 工程实现                           │
├──────────────────────┼────────────────────────────────────┤
│ 丘脑 Thalamus         │ Input Gateway：输入汇聚、分发、过滤   │
│ 视觉皮层              │ Vision 模块：图像理解、OCR、UI识别     │
│ 听觉皮层              │ Audio 模块：ASR、语音切分、说话人识别   │
│ Wernicke 区           │ NLU 层：意图识别、语义理解、任务判断    │
│ 前额叶                │ Planner + Orchestrator：规划、决策、调度│
│ 工作记忆              │ Blackboard State：上下文、证据、状态    │
│ 海马体                │ Episodic/Semantic Memory：记忆存储检索  │
│ 基底节                │ Policy + Tool Selector：动作选择         │
│ 小脑                  │ Executor：重试、纠偏、稳定化            │
│ 运动皮层              │ Tool Gateway：API/代码/数据库/浏览器     │
│ Broca 区              │ Response Composer：语言组织与输出        │
└──────────────────────┴────────────────────────────────────┘


## 复杂任务的时序图

图 4｜一次复杂任务的执行时序

用户
 │
 │  1. 提出任务：“分析问题，查资料，给结论，并执行后续动作”
 ▼
输入网关
 │
 │  2. 标准化输入，附加用户上下文
 ▼
语义理解层
 │
 │  3. 抽取目标/约束/风险
 │  4. 判断：这是开放式多步任务
 ▼
Planner LLM
 │
 │  5. 生成初始计划
 │    - 子任务A：搜集信息
 │    - 子任务B：分析对比
 │    - 子任务C：形成结论
 │    - 子任务D：执行动作
 ▼
Agent Orchestrator
 │
 ├──► Research Agent
 │      │
 │      ├──► 检索工具 / 浏览器 / 知识库
 │      │
 │      └──► 返回证据
 │
 ├──► Analysis Agent
 │      │
 │      ├──► 调用强推理 LLM
 │      │
 │      └──► 输出中间判断
 │
 ├──► Critic / Verifier
 │      │
 │      ├──► 检查证据不足？
 │      ├──► 检查逻辑跳步？
 │      └──► 不通过则退回重做
 │
 └──► Executor Agent
        │
        ├──► 通过权限网关
        ├──► 执行 API / DB / 邮件 / 代码动作
        └──► 写回结果

最终：
所有中间状态写入记忆系统
生成用户可读输出

## 企业架构图

图 5｜企业可部署版本

                  ┌────────────────────────────┐
                  │         Client Layer        │
                  │ Web / App / Voice / Devices │
                  └──────────────┬─────────────┘
                                 ▼
                  ┌────────────────────────────┐
                  │ API Gateway / Auth / Audit │
                  └──────────────┬─────────────┘
                                 ▼
        ┌────────────────────────────────────────────────┐
        │               Agent Runtime Layer              │
        │ Router | Planner | State Store | Scheduler     │
        │ Workflow Engine | Agent Loop | Policy Engine   │
        └──────────────┬───────────────┬────────────────┘
                       ▼               ▼
        ┌──────────────────────┐   ┌──────────────────────┐
        │    Model Layer        │   │    Memory Layer       │
        │ small LLM / big LLM   │   │ cache / vector / graph│
        │ VLM / ASR / TTS       │   │ episodic / semantic   │
        └──────────────┬───────┘   └──────────────┬───────┘
                       ▼                          ▼
        ┌────────────────────────────────────────────────┐
        │               Tool Execution Layer             │
        │ Browser | Code Sandbox | DB | CRM | ERP | RPA │
        │ Email | Search | Calendar | Internal APIs      │
        └────────────────────────────────────────────────┘
